torch==2.13.0
torchvision==0.18.0
torchaudio==2.3.0
diffusers==0.38.0
transformers==5.10.1
accelerate==0.32.0
omegaconf==2.3.0
einops==0.8.0
safetensors==0.4.3
pillow==12.3.0
numpy==1.26.4
pyyaml==6.0.1
requests==2.33.0
tqdm==4.66.4
opencv-python==4.10.0.84
imageio==2.35.1
scipy==1.14.1

# Для WebUI (Automatic1111) — обновлённые версии для Python 3.12
gradio==6.15.1
flask==3.1.3
websockets==12.0
aiohttp==3.14.3
uvicorn==0.30.6
fastapi==0.112.0

# Для ControlNet — используем последнюю совместимую версию для Python 3.12
controlnet_aux==0.0.10

# Для ESRGAN (апскейл) — альтернатива real-esrgan
realesrgan==0.3.0
facexlib==0.3.0

# Опционально: для ComfyUI
comfyui-cli==0.1.1

# Long-prompt encoding for SDXL / attention weights
compel>=2.0.2
